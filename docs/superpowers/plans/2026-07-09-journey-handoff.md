# Handoff: достроить «Путешествие Odyssey» (ветка feature/journey-map)

**Роль исполнителя:** senior-инженер, работаешь в `~/projects/patchquest`, ветка `feature/journey-map` уже создана и активна. Спека утверждена владельцем: `docs/superpowers/specs/2026-07-09-journey-map-design.md` — прочитай её ПЕРВОЙ. Плюс канон: `game/Characters.md` (формулы речи), предыдущие спеки в `docs/superpowers/specs/`.

**Цель:** играбельное «Путешествие» на проде: карта Земля→МКС→Луна(+ветка ИИ→Марс) с узлами-миссиями, 12 миссий × 5 обязательных заданий (+бонус), аварии SOS, interlude-диалоги, тип «впиши ответ». Успех = e2e зелёный на проде на 390×844 и 360×800.

## Уже сделано (не переделывай)

- Контракты: `Journey/JourneyStage/JourneyNode` + `validateJourney`, `CrewLine` + `interlude` на задании, тип `predict-output-write`, флаг `bonus` (валидатор: tasks 1..6, bonus один и последний, proof последний из обязательных). Всё в `packages/contracts/src/content.ts`, тесты зелёные.
- Движок (`apps/web/src/missions/engine.ts`): `checkAnswer` нормализует вписанный вывод; событие `skip-bonus`; бонус с hintStage=0 → `transfers`; тесты зелёные (31).
- Арт карты: `assets/journey-map.png` (Земля внизу → МКС → Луна с базой → развилка: прямая тропа вверх к Марсу = ветка ИИ; правая петля = декор/будущая глава 2).
- Портреты `apps/web/src/assets/characters/{kora,pix,vega}.webp`, `characterArt` в loader.

## Осталось (по порядку; коммит после каждого пункта; TDD где есть логика)

1. **`content/journey.json`** по схеме Journey. Этапы: `earth-iss` «Земля → МКС», `iss-moon` «МКС → Луна», `moon-mars` «Луна → Марс (Бортовой ИИ)». Узлы (порядок = порядок прохождения): code-01, code-02, SOS-01(emergency), code-03 | code-04, SOS-02(emergency), code-05, code-06 | agent-01, SOS-03(emergency), agent-02, agent-03. Координаты x/y в % подбери оверлеем по карте (Земля y≈85, МКС y≈66, Луна y≈45, Марс y≈8; тропа видна на арте) — сгенерируй скриншот с нодами и подгони на глаз.
2. **Валидатор**: в `scripts/validate-content.mjs` добавить проверку journey.json (`validateJourney` + missionIds из missionOrder). В `apps/web/src/content/loader.ts` — экспорт `journey`. В `scripts/optimize-art.mjs` — journey-map.png → `apps/web/src/assets/journey/journey-map.webp` (ширина 1080, лимит 350KB).
3. **Контент, самый большой кусок.** Все 9 миссий переписать до 5 обязательных заданий + interlude на 2–3 заданиях + бонус-испытание (6-е, `bonus:true`); 3 новые SOS-миссии (`sos-01..03`, code "SOS-01".., 5 заданий + бонус, `contextTag:"new"`, сектор: sos-01/02 → code, sos-03 → agent; skillId: code.variables / code.conditions / agent.verification; темы: кислородный контроллер / автопилот стыковки / бортовой ИИ галлюцинирует). Требования к качеству (из анализа обучения, владелец утвердил): в каждой миссии ≥1 задание `predict-output-write`; неправильные варианты = реальные заблуждения новичков, `explain` разбирает их; interlude-реплики само-объясняющие (KORA: «а что если бы там стояло >=?»), PIX — юмор поверх смысла; бонус — злая вариация той же темы (edge case). Тон: взрослый, без «молодец». Добавить sos-миссии в `content/index.json` missionOrder (code: +sos-01,sos-02 в конце? НЕТ — missionOrder оставить как есть для карты навыков, а порядок прохождения задаёт journey.json; в missionOrder добавить sos-миссии в их сектора в конец списка, карта навыков дедуплицирует по skillId). **Поднять contentVersion** в index.json и `APP_CONTENT_VERSION` в storage.ts (smoke сверяет).
4. **UI**: компонент `apps/web/src/journey/JourneyMap.tsx` — вертикальное полотно (img карты, скролл к текущему узлу, старт снизу), узлы-кнопки по x/y%, состояния: locked(замок)/active(пульс, Motion)/completed(✓)/skipped(«уже знаешь»)/emergency(⚠, alert-стиль); маркер корабля у последнего пройденного; тап active→миссия, locked→подсказка «сначала …». «Миссия на сегодня» — закреплённая карточка снизу. App.tsx: палубы «Путешествие» (карта = главная) / «Карта навыков» / «Профиль»; убрать sector-switch; рекомендация = первый непройденный-нескипнутый узел journey; unlock-цепочка (узел открыт, если все предыдущие completed или skipped; при skipsOverridden скипы не считаются пройденными). Calibration.tsx: убрать выбор сектора → кнопка «В путь» (ставит sector:"code"). MissionPlayer: рендерер WriteTask (input + Проверить, `inputmode` по содержимому), бонус-оффер («Испытание KORA: принять / Завершить миссию» → skip-bonus), interlude-реплики с портретами на экране разбора, alert-шапка для emergency (передавай флаг из journey node). AtlasMap: единый список навыков (дедуп по skillId из всех миссий journey). Прогрессивное раскрытие сохранить (док после 1-й пройденной).
5. **kora-worker**: перегенерить hint-safe контекст `npm run build:context --workspace @orbitquest/kora-worker` (interlude/bonus НЕ должны попадать с ответами — проверь генератор: он берёт только prompt/code/codeB — ок).
6. **Проверки**: `npm run typecheck && npm test && node --experimental-strip-types scripts/validate-content.mjs --strict && npm run build && npm run smoke`. e2e: перепиши `<scratchpad>/e2e-verify.mjs` (путь: пролог → калибровка → «В путь» → авто-вход в code-01 → пройти с ошибкой/подсказкой/write-заданием → бонус пропустить → карта: узел code-02 активен, SOS-01 залочен → пройти code-02 → SOS-01 открылся с ⚠). Прогнать 390×844 и 360×800 через `npx vite preview --port 4199` (chromium channel:"chrome", playwright установлен в scratchpad). Скриншоты карты и SOS-миссии сохранить в scratchpad/shots.
7. **Деплой**: merge в main (--no-ff), push (`env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy git push`), дождаться CI (`gh run watch`, тоже без прокси-переменных), e2e по https://evgenijtebevezet-art.github.io/orbitquest/.
8. **Cloudflare (если владелец завершил `wrangler login` — проверь `npx wrangler whoami`):** в `apps/kora-worker`: `npx wrangler kv namespace create LIMITS` → id в wrangler.toml; `CLIENT_KEY` сгенерируй сам (random 32 hex) и поставь через `echo <key> | npx wrangler secret put CLIENT_KEY`; NIM_API_KEY НЕ трогай (владелец вставит сам — попроси в итоговом сообщении); `npx wrangler deploy`; `gh variable set KORA_URL --body <url>` и `KORA_CLIENT_KEY`; перезапустить workflow. Если whoami не залогинен — пропусти весь пункт, отметь в отчёте.
9. **Беклог**: создай `docs/superpowers/specs/2026-07-09-blitz-multiplayer-backlog.md` — кратко: соло-блиц «Метеоритный дождь» (метеор=вопрос из пройденного, таймер, 3 щита, бафы за стрик, арена у МКС после этапа 1), async-«вызов» (seed в URL), duo-кооп через CF Durable Objects (волны вместе/поодиночке/вместе, общие щиты, дарение бафов), PvP-гонка; банк цитат `content/quotes.json` (30–50 куратор. цитат: Дейкстра/Кнут/Хоппер/Керниган/Циолковский, VEGA приносит между миссиями). Пометь status: backlog.

## Жёсткие правила

- Git: коммит после каждого пункта, явные пути (никогда `-A`), сообщения `feat:/content:/fix:/docs:`. Секреты не коммитить. Прод-пайплайны не запускать локально.
- Никаких вопросов владельцу — все решения в спеке; если противоречие, выбери консервативно и отметь в отчёте.
- Не выдавай завершение без зелёных проверок; частичный результат — так и напиши.
- Пайпы глотают exit-коды (`cmd | tail` вернёт 0) — проверяй `; echo EXIT=$?` отдельно.
- Windows: кейс-инсенситив ФС (не создавай файлы, отличающиеся регистром); после добавления workspace — `npm install` (lock!).
- e2e-скрипты и playwright живут в scratchpad сессии: `C:\Users\gento\AppData\Local\Temp\claude\C--Users-gento\a3ed669b-7131-415c-90b9-f05f3bb2cb71\scratchpad`.

## Стоп-условия

Остановись и доложи, если: карта-арт не позволяет разумно разместить узлы (нужна перегенерация — codex-image skill, референсы в assets/); прод-CI красный дважды подряд по одной причине; wrangler требует интерактива, которого нет.
